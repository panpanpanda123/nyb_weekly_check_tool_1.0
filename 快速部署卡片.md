# 🚀 活动参与度功能 - 快速部署卡片

## 📦 第一步：上传Excel文件

```bash
# 从本地上传到服务器
scp "活动参与度3月9日.xlsx" root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/promo_data/
scp "3月活动门店表.xlsx" root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/promo_data/
```

## 🔄 第二步：服务器操作

```bash
# 1. SSH登录
ssh root@139.224.200.133

# 2. 进入项目目录
cd /var/www/nyb_weekly_check_tool_1.0

# 3. 拉取最新代码
git pull

# 4. 运行部署脚本
chmod +x deploy_promo.sh
./deploy_promo.sh

# 5. 查看服务状态
sudo systemctl status review-viewer
```

## ✅ 第三步：验证

1. 浏览器访问：https://weeklycheck.blitzepanda.top/
2. 清除缓存（Ctrl+F5）
3. 应该看到4个导航选项卡
4. 点击"📊 活动参与度"

## 🔧 日常更新（每天）

```bash
# 1. 上传新的Excel文件
scp "活动参与度3月10日.xlsx" root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/promo_data/

# 2. SSH登录服务器
ssh root@139.224.200.133

# 3. 运行导入脚本
cd /var/www/nyb_weekly_check_tool_1.0
python3 import_promo_data.py
```

## 🐛 故障排查

### 导航按钮不显示
```bash
# 清除浏览器缓存（Ctrl+F5）
```

### 数据为空
```bash
# 检查文件
ls -lh promo_data/

# 手动导入
python3 import_promo_data.py

# 查看日志
tail -f logs/rating_app.log
```

### 服务异常
```bash
# 重启服务
sudo systemctl restart review-viewer

# 查看状态
sudo systemctl status review-viewer

# 查看日志
sudo journalctl -u review-viewer -f
```

## 📞 需要帮助？

查看完整文档：`活动参与度部署指南.md`

---

**提示**：Nginx配置无需修改，Flask自动处理路由！
