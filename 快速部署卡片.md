# 🚀 审核结果展示系统 - 快速部署卡片

## 📋 部署前准备

- [ ] Ubuntu 22.04 服务器（2核/4GB/20GB）
- [ ] 开放 80 端口
- [ ] 代码已推送到 GitHub

## ⚡ 3步部署

### 1️⃣ 推送代码
```bash
git push origin main
```

### 2️⃣ 服务器安装
```bash
ssh root@服务器IP
wget https://raw.githubusercontent.com/你的用户名/你的仓库/main/deploy/install.sh
sudo bash install.sh
# 输入 GitHub 仓库地址
```

### 3️⃣ 访问系统
```
http://服务器IP
```

---

## 📱 使用地址

| 功能 | 地址 |
|------|------|
| 查看审核结果 | `http://服务器IP/` |
| 数据管理 | `http://服务器IP/admin/upload` |

---

## 🔧 常用命令

```bash
# 查看状态
sudo systemctl status review-viewer

# 查看日志
sudo journalctl -u review-viewer -f

# 重启服务
sudo systemctl restart review-viewer

# 更新代码
cd /opt/review-result-viewer
sudo bash deploy/update.sh
```

---

## 📊 数据上传流程

1. **先上传白名单** (`.xlsx`)
   - 包含门店的战区/省份/城市信息
   
2. **再上传审核结果** (`.csv`)
   - 从本地审核系统导出的CSV文件

---

## ⚠️ 注意事项

- ✅ 白名单必须先上传
- ✅ CSV文件从本地审核系统导出
- ✅ 数据库凭据在 `/root/.review_viewer_db_credentials`
- ✅ 服务会自动启动，无需手动操作

---

## 🆘 遇到问题？

```bash
# 查看详细日志
sudo journalctl -u review-viewer -n 100 --no-pager

# 检查数据库
cat /root/.review_viewer_db_credentials

# 检查 Nginx
sudo nginx -t
sudo systemctl status nginx
```

---

**部署时间**: 约 5-10 分钟 ⏱️
