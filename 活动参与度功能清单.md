# 活动参与度监控功能清单

## ✅ 已实现功能

### 1. 数据库模型 ✅
- [x] PromoParticipation 表（活动参与度数据）
- [x] PromoImportLog 表（导入日志）
- [x] 索引优化（门店ID、战区、区域经理、数据日期）

**文件**：`shared/database_models.py`

---

### 2. 数据导入脚本 ✅
- [x] 自动查找最新的活动参与度文件
- [x] 从文件名提取日期
- [x] 加载活动门店表（筛选参加活动的门店）
- [x] 关联whitelist获取区域经理
- [x] 清空旧数据（同一日期）
- [x] 导入新数据
- [x] 记录导入日志

**文件**：`import_promo_data.py`

**支持的文件名格式**：
- `活动参与度3月9日.xlsx`
- `活动参与度2026年3月9日.xlsx`

**数据提取**：
- A列：门店ID
- F列：订单量
- I列：权益卡销量
- J列：活动套餐销量
- K列：活动参与度

---

### 3. 后端API ✅
- [x] `/promoratio` - 页面路由
- [x] `/api/promo/filters` - 获取筛选选项（战区、数据日期）
- [x] `/api/promo/regional-managers` - 根据战区获取区域经理
- [x] `/api/promo/all-regional-managers` - 获取所有区域经理
- [x] `/api/promo/search` - 搜索门店数据（支持筛选、排序、分页）
- [x] `/api/promo/export` - 导出Excel

**文件**：`viewer/app_viewer.py`

**筛选功能**：
- 战区筛选
- 区域经理筛选（级联）
- 门店搜索（ID或名称）

**排序功能**：
- 活动参与度（默认）
- 订单量
- 权益卡销量
- 活动套餐销量
- 升序/降序

---

### 4. 前端页面 ✅
- [x] HTML模板（复用equipment样式）
- [x] CSS样式（响应式设计）
- [x] JavaScript交互逻辑

**文件**：
- `viewer/templates/promoratio.html`
- `viewer/static/promoratio.css`
- `viewer/static/promoratio.js`

**页面功能**：
- 顶部导航（周清审核 | 门店评级 | 设备异常 | 活动参与度）
- 筛选区域（战区、区域经理、门店搜索、排序方式）
- 数据时间提示
- 统计信息（门店总数）
- 门店列表（卡片式展示）
- 分页功能
- 导出按钮

**门店卡片展示**：
- 门店名称和ID
- 战区和区域经理
- 活动参与度（大号徽章）
- 订单量
- 权益卡销量
- 活动套餐销量

---

### 5. 文档和脚本 ✅
- [x] 使用说明文档
- [x] 数据文件夹README
- [x] 部署脚本
- [x] 功能清单

**文件**：
- `活动参与度监控说明.md`
- `promo_data/README.md`
- `deploy_promo.sh`
- `活动参与度功能清单.md`

---

## 📋 数据流程

```
1. Excel文件准备
   ├── 活动参与度MM月DD日.xlsx
   └── 3月活动门店表.xlsx

2. 数据导入
   ├── 读取活动参与度Excel（A、F、I、J、K列）
   ├── 筛选活动门店表（第一列门店ID）
   ├── 关联whitelist（获取门店名称、战区、区域经理）
   └── 存入数据库（promo_participation表）

3. 网页展示
   ├── 加载筛选选项（战区、区域经理）
   ├── 搜索门店数据（支持筛选、排序、分页）
   └── 展示门店卡片（活动参与度、订单量等）

4. 数据导出
   └── 导出Excel文件（包含所有字段）
```

---

## 🎨 设计特点

### 1. 复用现有样式
- 使用 `viewer.css` 基础样式
- 新增 `promoratio.css` 专用样式
- 保持与equipment页面一致的风格

### 2. 响应式设计
- 支持PC和手机访问
- 自适应布局
- 移动端优化

### 3. 用户体验
- 清晰的数据展示
- 直观的排序功能
- 快速的筛选操作
- 流畅的分页切换

---

## 🔧 技术栈

### 后端
- Python 3
- Flask
- SQLAlchemy
- PostgreSQL
- pandas（Excel处理）
- openpyxl（Excel导出）

### 前端
- HTML5
- CSS3
- JavaScript (ES6+)
- Fetch API

---

## 📊 数据库表

### promo_participation
```sql
CREATE TABLE promo_participation (
    id SERIAL PRIMARY KEY,
    store_id VARCHAR(50) NOT NULL,
    store_name VARCHAR(255),
    war_zone VARCHAR(50),
    regional_manager VARCHAR(50),
    order_count INTEGER DEFAULT 0,
    benefit_card_sales INTEGER DEFAULT 0,
    promo_package_sales INTEGER DEFAULT 0,
    participation_rate VARCHAR(20),
    data_date VARCHAR(20),
    import_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_promo_store_id ON promo_participation(store_id);
CREATE INDEX idx_promo_war_zone ON promo_participation(war_zone);
CREATE INDEX idx_promo_regional_manager ON promo_participation(regional_manager);
CREATE INDEX idx_promo_data_date ON promo_participation(data_date);
```

### promo_import_log
```sql
CREATE TABLE promo_import_log (
    id SERIAL PRIMARY KEY,
    data_date VARCHAR(20),
    import_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    records_count INTEGER
);
```

---

## 🚀 部署步骤

### 1. 上传文件到服务器
```bash
# 上传代码文件
scp shared/database_models.py root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/shared/
scp import_promo_data.py root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/
scp viewer/app_viewer.py root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/viewer/
scp viewer/templates/promoratio.html root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/viewer/templates/
scp viewer/static/promoratio.* root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/viewer/static/
scp deploy_promo.sh root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/

# 上传数据文件
scp 活动参与度3月9日.xlsx root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/promo_data/
scp 3月活动门店表.xlsx root@139.224.200.133:/var/www/nyb_weekly_check_tool_1.0/promo_data/
```

### 2. 运行部署脚本
```bash
ssh root@139.224.200.133
cd /var/www/nyb_weekly_check_tool_1.0
chmod +x deploy_promo.sh
./deploy_promo.sh
```

### 3. 验证部署
```bash
# 检查服务状态
sudo systemctl status review-viewer

# 查看日志
tail -f logs/rating_app.log

# 访问页面
# https://weeklycheck.blitzepanda.top/promoratio
```

---

## 📝 使用场景

### 场景1：区域经理查看自己负责的门店
1. 打开页面
2. 选择自己的名字
3. 查看门店列表
4. 按活动参与度排序
5. 找出参与度低的门店进行督促

### 场景2：管理层查看整体情况
1. 不选择筛选条件
2. 查看所有门店
3. 按战区对比
4. 导出数据进行分析

### 场景3：每天更新数据
1. 管理员上传最新的Excel文件
2. 运行导入脚本
3. 区域经理查看最新数据

---

## ⚠️ 注意事项

1. **数据文件**：必须放在 `promo_data/` 文件夹中
2. **文件命名**：必须包含"活动参与度"和日期
3. **数据更新**：每次导入会清空同一日期的旧数据
4. **区域经理匹配**：依赖whitelist表，需要先导入白名单
5. **活动门店表**：可选，如果没有会导入所有门店

---

## 🎉 总结

活动参与度监控系统已完整实现，包括：
- ✅ 数据库模型和表结构
- ✅ 数据导入脚本（自动化）
- ✅ 后端API（筛选、排序、分页、导出）
- ✅ 前端页面（响应式、用户友好）
- ✅ 完整文档和部署脚本

系统可以：
- 自动导入Excel数据
- 关联多个表（活动参与度、活动门店、whitelist）
- 提供灵活的筛选和排序
- 支持数据导出
- 移动端友好

**下一步**：部署到服务器并测试完整流程

---

**创建人**：Kiro AI Assistant  
**创建日期**：2026-03-09  
**版本**：v1.0
