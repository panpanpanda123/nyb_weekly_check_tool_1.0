# 🚀 服务器部署操作指南

## 📍 服务器信息
- **IP地址**: 172.22.181.185
- **系统**: Ubuntu 22.04.5 LTS ✅
- **仓库**: https://github.com/panpanpanda123/nyb_weekly_check_tool_1.0.git

---

## 第一步：本地推送代码

在你的**本地电脑**上打开命令行，运行：

```bash
git add .
git commit -m "准备部署"
git push origin main
```

---

## 第二步：服务器安装（你已经登录了）

**直接复制粘贴以下命令：**

```bash
# 下载安装脚本
cd ~
wget https://raw.githubusercontent.com/panpanpanda123/nyb_weekly_check_tool_1.0/main/deploy/install.sh

# 给脚本执行权限
chmod +x install.sh

# 运行安装（会提示输入仓库地址）
bash install.sh
```

**当看到提示 "请输入 GitHub 仓库 URL:" 时，输入：**
```
https://github.com/panpanpanda123/nyb_weekly_check_tool_1.0.git
```

然后等待安装完成（约5-10分钟）

---

## 第三步：开放端口（如果无法访问）

如果安装完成后无法访问网站，需要在阿里云控制台开放80端口：

1. 登录 https://ecs.console.aliyun.com
2. 找到你的ECS实例
3. 点击"安全组" → "配置规则"
4. 点击"添加安全组规则"
5. 填写：
   - 端口范围：80/80
   - 授权对象：0.0.0.0/0
   - 描述：HTTP访问
6. 点击"确定"

---

## 第四步：访问系统

在浏览器打开：

- **展示页面**: http://172.22.181.185
- **管理页面**: http://172.22.181.185/admin/upload

---

## 📊 使用流程

### 管理员上传数据

1. 打开管理页面：http://172.22.181.185/admin/upload
2. **先上传白名单**（检查项记录.xlsx）
3. **再上传审核结果**（审核结果_日期.csv）

### 外部同事查看

1. 打开展示页面：http://172.22.181.185
2. 选择筛选条件（战区/省份/城市/门店标签/是否合格）
3. 点击"搜索"查看结果

---

## 🔧 常用命令

```bash
# 查看服务状态
systemctl status review-viewer

# 查看实时日志
journalctl -u review-viewer -f

# 重启服务
systemctl restart review-viewer

# 查看数据库密码
cat /root/.review_viewer_db_credentials
```

---

## ⚠️ 注意事项

1. **必须先上传白名单**，再上传审核结果
2. 白名单文件格式：`.xlsx`
3. 审核结果文件格式：`.csv`（从本地审核系统导出）
4. 如果上传失败，检查文件格式是否正确

---

## 🆘 遇到问题？

### 安装失败
```bash
# 查看详细错误
cat /var/log/syslog | tail -50
```

### 服务无法启动
```bash
# 查看服务日志
journalctl -u review-viewer -n 100 --no-pager
```

### 无法访问网站
```bash
# 检查服务是否运行
systemctl status review-viewer
systemctl status nginx

# 检查端口是否开放
netstat -tlnp | grep 80
```

---

**准备好了就开始吧！** 🎉
