# 部署检查清单 - 2026年3月9日更新

## ✅ 部署前检查

- [ ] 已备份当前代码
- [ ] 已阅读 `CHANGELOG_20260309.md`
- [ ] 已阅读 `设备异常判定逻辑说明.md`
- [ ] 确认服务器连接正常

---

## 📦 部署步骤

### 1. 连接服务器
```bash
ssh root@139.224.200.133
```

### 2. 进入项目目录
```bash
cd /var/www/nyb_weekly_check_tool_1.0
```

### 3. 备份当前代码
```bash
git add .
git commit -m "Backup before update 20260309"
git status
```

### 4. 拉取最新代码
```bash
git pull origin main
```

### 5. 检查文件是否更新
```bash
# 检查关键文件的修改时间
ls -lh equipment_utils.py
ls -lh equipment_config.py
ls -lh 设备异常判定逻辑说明.md
ls -lh CHANGELOG_20260309.md
```

### 6. 重启服务
```bash
sudo systemctl restart review-viewer
```

### 7. 检查服务状态
```bash
sudo systemctl status review-viewer
```

**预期输出**：
- Active: active (running)
- 没有错误信息

### 8. 查看日志
```bash
tail -f logs/rating_app.log
```

**预期输出**：
- 没有Python错误
- 服务正常启动

---

## 🧪 部署后测试

### 测试1：访问页面
```bash
# 在浏览器中打开
https://weeklycheck.blitzepanda.top/equipment
```

**预期结果**：
- [ ] 页面正常加载
- [ ] 筛选器正常工作
- [ ] 门店列表正常显示

### 测试2：测试判定逻辑（可选）
```bash
# 在服务器上运行测试脚本
cd /var/www/nyb_weekly_check_tool_1.0
python3 测试判定逻辑.py [门店ID]
```

**预期结果**：
- [ ] 脚本正常运行
- [ ] 显示判定结果
- [ ] 显示未处理日期（如果有）

### 测试3：导入数据测试
```bash
# 运行一次数据导入
python3 import_equipment_data.py
```

**预期结果**：
- [ ] 导入成功
- [ ] 没有错误信息
- [ ] 快照创建成功

### 测试4：导出功能测试
```
1. 在网页上点击"导出"按钮
2. 下载Excel文件
3. 检查文件内容
```

**预期结果**：
- [ ] 文件正常下载
- [ ] 包含"未处理日期"列
- [ ] 数据完整准确

---

## 🔍 问题排查

### 问题1：服务启动失败
```bash
# 查看详细错误
sudo journalctl -u review-viewer -n 50

# 检查Python语法错误
python3 -m py_compile equipment_utils.py
python3 -m py_compile viewer/app_viewer.py
```

### 问题2：导入数据失败
```bash
# 手动运行导入脚本，查看详细错误
python3 import_equipment_data.py
```

### 问题3：页面显示异常
```bash
# 清除浏览器缓存
# 强制刷新页面（Ctrl+F5 或 Cmd+Shift+R）

# 检查API响应
curl https://weeklycheck.blitzepanda.top/api/equipment/filters
```

### 问题4：判定逻辑不正确
```bash
# 使用测试脚本检查
python3 测试判定逻辑.py [门店ID]

# 查看数据库中的快照数据
# 连接数据库查询
```

---

## 📋 回滚步骤（如果需要）

### 1. 回滚代码
```bash
cd /var/www/nyb_weekly_check_tool_1.0
git log --oneline -5
git reset --hard [上一个commit的hash]
```

### 2. 重启服务
```bash
sudo systemctl restart review-viewer
```

### 3. 验证服务
```bash
sudo systemctl status review-viewer
```

---

## 📞 联系方式

如果遇到问题，请联系：
- **技术支持**：[您的联系方式]
- **紧急联系**：[紧急联系方式]

---

## ✅ 部署完成确认

部署完成后，请确认以下事项：

- [ ] 服务正常运行
- [ ] 页面正常访问
- [ ] 筛选功能正常
- [ ] 导出功能正常
- [ ] 判定逻辑正确
- [ ] 没有错误日志
- [ ] 已通知区域经理新功能

---

**部署人**：___________  
**部署日期**：___________  
**部署时间**：___________  
**部署结果**：□ 成功  □ 失败  □ 需要回滚

**备注**：
___________________________________________
___________________________________________
___________________________________________
