# 紧急修复指令 - 设备分类处理功能

## 问题
数据库表 `equipment_processing` 缺少 `equipment_type` 字段，导致 500 错误。

## 立即执行（复制粘贴）

### 1. 连接服务器
```bash
ssh root@139.224.200.133
```

### 2. 进入项目目录
```bash
cd /var/www/nyb_weekly_check_tool_1.0
```

### 3. 确认迁移脚本已上传
```bash
ls -lh migrate_equipment_processing.py
```

如果文件不存在，需要先用 WinSCP 上传 `migrate_equipment_processing.py`

### 4. 运行迁移脚本
```bash
python3 migrate_equipment_processing.py
```

预期输出：
```
============================================================
开始迁移 equipment_processing 表...
============================================================

1. 清空现有处理记录（因为需要添加必需字段）...
   ✓ 已清空处理记录

2. 添加 equipment_type 列...
   ✓ 已添加 equipment_type 列

3. 创建复合索引...
   ✓ 已创建索引 idx_processing_store_type

4. 移除默认值...
   ✓ 已移除默认值

============================================================
✅ 迁移完成！
============================================================
```

### 5. 重启服务
```bash
sudo systemctl restart review-viewer
```

### 6. 验证服务状态
```bash
sudo systemctl status review-viewer
```

### 7. 测试功能
访问: https://weeklycheck.blitzepanda.top/equipment

搜索门店，确认不再报错。

## 如果迁移脚本不存在

### 方案 A: 手动执行 SQL（最快）

```bash
# 连接数据库
psql -U postgres configurable_ops
```

然后执行以下 SQL：

```sql
-- 清空旧数据
DELETE FROM equipment_processing;

-- 添加字段
ALTER TABLE equipment_processing 
ADD COLUMN equipment_type VARCHAR(50) NOT NULL DEFAULT 'POS';

-- 创建索引
CREATE INDEX IF NOT EXISTS idx_processing_store_type 
ON equipment_processing(store_id, equipment_type);

-- 移除默认值
ALTER TABLE equipment_processing 
ALTER COLUMN equipment_type DROP DEFAULT;

-- 退出
\q
```

然后重启服务：
```bash
sudo systemctl restart review-viewer
```

### 方案 B: 上传迁移脚本后执行

1. 用 WinSCP 上传 `migrate_equipment_processing.py` 到 `/var/www/nyb_weekly_check_tool_1.0/`
2. 执行上面的步骤 4-7

## 验证修复成功

1. 访问 https://weeklycheck.blitzepanda.top/equipment
2. 选择战区和区域经理
3. 点击搜索
4. 应该能正常显示设备列表，不再报 500 错误

## 如果还有问题

查看错误日志：
```bash
tail -50 /var/www/nyb_weekly_check_tool_1.0/logs/rating_app.log
```

或者：
```bash
journalctl -u review-viewer -n 50
```

## 注意事项

- 迁移会清空所有旧的处理记录
- 区域经理需要重新处理设备异常
- 这是正常的，因为新增了必需字段

## 完成后

功能应该恢复正常，设备会按 POS 和机顶盒分组显示，可以分别处理。
