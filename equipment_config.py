"""
设备异常监控配置文件
Equipment Monitoring Configuration
"""

# ==================== 数据保留配置 ====================
# 快照保留天数（只保留最近N天的数据，自动清理旧数据）
SNAPSHOT_RETENTION_DAYS = 10

# 处理记录保留天数（只保留最近N天的数据，自动清理旧数据）
PROCESSING_RETENTION_DAYS = 10

# ==================== 经常出问题判定规则 ====================
# 判定规则：满足以下任一条件即标记为"经常出问题"

# 规则1: 当天反复出问题（优先级最高）
# 上午有异常 + 标记"已恢复" + 下午又异常 = 立即标记"多次出问题（当天反复）"
ENABLE_SAME_DAY_REPEAT_CHECK = True

# 规则2: 未及时处理（优先级第二）
# 上午有异常 + 下午有异常 + 没有处理记录 = 标记"多次出问题（未及时处理）"
# 同时记录所有未处理的日期，用于导出统计
# （由功能开关区的 ENABLE_UNPROCESSED_CHECK 控制）

# 规则3: 历史统计规则（优先级第三）
# 格式：{'days': 天数, 'threshold': 异常次数阈值}
CHRONIC_RULES = [
    {'days': 5, 'threshold': 3},   # 5天内出现3次异常
    {'days': 10, 'threshold': 4},  # 10天内出现4次异常
]

# 特殊情况处理：
# - 如果标记"未恢复"且填写了预计恢复日期：会被should_suppress()过滤，不显示在列表中
# - 如果标记"未恢复"但没填预计恢复日期：继续显示在列表中，但不触发chronic标记

# ==================== 预计恢复配置 ====================
# 预计恢复最长天数（限制区域经理填写的预计恢复时间）
EXPECTED_RECOVERY_MAX_DAYS = 7

# ==================== 检查时段配置 ====================
# 上午检查时间分界点（小时，24小时制）
# 小于此时间的导入算上午(AM)，大于等于此时间的算下午(PM)
AM_PM_BOUNDARY_HOUR = 14  # 14点（下午2点）

# ==================== 功能开关 ====================
# 是否启用POS历史追踪（机顶盒不启用）
ENABLE_POS_HISTORY_TRACKING = True

# 是否自动清理过期快照
AUTO_CLEANUP_OLD_SNAPSHOTS = True

# 是否启用多日统计（经常出问题判定：5天3次、10天4次等历史规则）
# 关闭后只保留"当天反复"检测，不统计跨天的异常次数
ENABLE_MULTI_DAY_CHRONIC = False

# 是否启用"未及时处理"检测（上午+下午都异常但没有处理记录）
ENABLE_UNPROCESSED_CHECK = False

# ==================== 说明 ====================
"""
配置说明：

1. SNAPSHOT_RETENTION_DAYS (10天)
   - 保留最近10天的快照数据
   - 每天最多2次检查（上午+下午）
   - 总数据量：10天 × 2次 × 门店数 × 设备类型
   - 自动清理超过10天的旧数据，节省空间

2. PROCESSING_RETENTION_DAYS (10天)
   - 保留最近10天的处理记录
   - 自动清理超过10天的旧记录，节省空间
   - 如果休息期间没有维护，缺失的日期会被自动忽略
   - 只保留最近10个自然日的数据

3. CHRONIC_RULES (经常出问题判定)
   - 规则1（优先）：当天上午有异常 + 标记"已恢复" + 下午又异常 = 立即标记"多次出问题（当天反复）"
   - 规则2（优先）：上午有异常 + 下午有异常 + 没有处理 = 标记"多次出问题（未及时处理）"
     * 同时记录所有未处理的日期（最近10天），导出时显示在"未处理日期"列
     * 用于追踪区域经理休假期间未处理的门店
   - 规则3：5天内（最多10次检查）出现>=3次异常
   - 规则4：10天内（最多20次检查）出现>=4次异常
   - 满足任一规则即标记为"经常出问题"
   - 可以添加更多规则，格式相同
   
   特殊情况：
   - 标记"未恢复"+填写预计恢复日期：在预计恢复期内不显示在列表中（被should_suppress()过滤）
   - 标记"未恢复"+没填预计恢复日期：继续显示在列表中，但不触发chronic标记

4. EXPECTED_RECOVERY_MAX_DAYS (7天)
   - 区域经理填写预计恢复时间时，最长只能填7天
   - 在预计恢复日期前，该门店不会再次提示异常
   - 到期后自动恢复提示

5. AM_PM_BOUNDARY_HOUR (14点)
   - 14点前的导入算上午检查(AM)
   - 14点后的导入算下午检查(PM)
   - 根据实际检查时间调整（10:30和17:00）

6. 功能开关
   - ENABLE_POS_HISTORY_TRACKING: 只对POS启用历史追踪
   - AUTO_CLEANUP_OLD_SNAPSHOTS: 自动清理旧快照

修改配置后需要重启服务生效。
"""
